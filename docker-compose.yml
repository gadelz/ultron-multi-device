services:
  gateway:
    build: ./src/gateway
    ports:
      - "8080:8080"
    environment:
      - ULTRON_API_KEY=changeme-secret-key
      - DATABASE_URL=sqlite:///./ultron.db
      - ULTRON_BIND=0.0.0.0
    volumes:
      - ./src/gateway:/app
    restart: unless-stopped
